class Order {
    constructor(
        book,
        userId,
        quantity,
    ) {
        this.book = book;
        this.userId = userId;
        this.quantity = quantity;
    }
}
export default Order;